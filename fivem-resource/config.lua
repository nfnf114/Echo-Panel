--[[
╔══════════════════════════════════════════════════════════════════════╗
║                  🔴 Echo Panel — إعدادات السكربت                  ║
║          يرجى قراءة كل سطر بعناية قبل تشغيل السكربت              ║
╚══════════════════════════════════════════════════════════════════════╝
]]

Config = {}

-- ══════════════════════════════════════════
--  1. مفتاح الرخصة
--  ┌─ هذا هو المفتاح الذي أرسله لك البوت على الديسكورد
--  └─ مثال: "SHF-PRM-ABCD-1234"
-- ══════════════════════════════════════════
Config.LicenseKey = "SHF-MAX-9GZX-QEU6" -- ✏️ ضع مفتاح رخصتك هنا

-- ══════════════════════════════════════════
--  2. رابط لوحة التحكم
--  ┌─ هذا هو رابط موقع Echo Panel
--  └─ لا تغيره إلا إذا طُلب منك ذلك
-- ══════════════════════════════════════════
Config.PanelURL = "http://localhost:3001" -- ✅ رابط الباك إند على نفس الجهاز

-- ══════════════════════════════════════════
--  3. إطار العمل (Framework)
--  ┌─ "qbcore" = إذا كان سيرفرك يعمل على QBCore
-- ══════════════════════════════════════════
Config.Framework = "qbcore" -- ✏️ اختر: "qbcore"

-- ══════════════════════════════════════════
--  4. مدة الإرسال التلقائي (بالثواني)
--  ┌─ كل كم ثانية يتصل السكربت باللوحة لإرسال
--  │  بيانات السيرفر (عدد اللاعبين، الحالة...)
--  └─ لا تجعلها أقل من 15 ثانية لتجنب الضغط
-- ══════════════════════════════════════════
-- ══════════════════════════════════════════
Config.HeartbeatInterval = 30 -- لا تغير هذا

-- ══════════════════════════════════════════
--  5. إعدادات التصوير المباشر (Screenshots)
--  ┌─ لضمان نجاح التقاط الشاشة بدون أخطاء، نستخدم Fivemanage
--  └─ ضع الـ API Token الخاص بك هنا (من fivemanage.com)
-- ══════════════════════════════════════════
Config.FivemanageToken = "" -- ✏️ ضع مفتاح Fivemanage هنا
